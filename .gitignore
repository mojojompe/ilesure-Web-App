node_modules
dist
.env

# Local overrides. `.env.local` was committed and pointed VITE_API_URL at
# http://localhost:4000, so every production build baked in localhost and the
# deployed app called the developer's machine.
.env*
!.env.example
